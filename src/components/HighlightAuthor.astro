---
interface Props {
  name: string;
  authors: string;
}

const { authors } = Astro.props;
const splitedAuthors = authors.split(", ");
---

<p class="leading-5 my-0">
  {
    splitedAuthors.map((item, index) => (
      <>
        {index > 0 && ", "}
        {item === "Congkai An" ? <strong>{item}</strong> : item}
      </>
    ))
  }
</p>
